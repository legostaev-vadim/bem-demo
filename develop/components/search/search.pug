mixin search(mix)
    form.search(class=mix action="#" method="POST")
        ._mask
            button._close(type="button")
            input._query(type="search" name="query" placeholder="enter your request" required)
        button._submit(type="submit")
            +symbol('search__symbol', 'search', '0 0 27 28')